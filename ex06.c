#include <stdio.h>

int main(){
    float base, altura;
    printf("Base: ");
    scanf("%f",&base);
    printf("Altura: ");
    scanf("%f",&altura);
    printf("Área do retângulo: %.2f", base*altura);
}